"""Core primitives shared by every M0 unit: sentinels, canonical JSON,
content digests, identity, coverage gaps, append-only revisions, receipts, and
the positive capability allowlist."""

from __future__ import annotations

__all__: list[str] = []
