# Architecture Decision Ledger — DRAFT

Status: `DRAFT` (Phase 1). Nothing here is frozen. Facilitator: Claude Fable 5, job `05b209ef`.
Every row classifies its source: `owner_decision`, `observed_evidence`, `reviewer_finding`, `engineering_proposal`, or `open_gate`.
A reviewer preference is never promoted to an Owner decision.

## A. Owner decisions already durable (imported, not re-asked)

| ID | Decision | Source | Date |
| --- | --- | --- | --- |
| O1 | One `Flight` = one A/B `Experiment`; rollout/exposure/window/run-attempt are observations under it | `owner-alignment-record.md` O1 | 2026-08-16 |
| O2 | Decision-metric set + frozen `DecisionMetricPolicy`; first M0 defaults to one metric; co-primary only as approved preregistered exception; no permanent singular cardinality | `owner-alignment-record.md` O2; ADR-0004 | 2026-08-16 |
| O3 | Invalid Experiment: typed guidance + reopen condition first; a correct, reviewable, unapplied validity/instrumentation/data-quality diff only after exact-target, authority, validator, and no-write delivery gates; never apply | `owner-alignment-record.md` O3; ADR-0005 | 2026-08-16 |
| O4 | Experiment Owner runs; Independent DS Consultant challenges (no approval); Experiment Review Committee decides pass/change/block; fixture-only role overlap allowed, never for a production Flight | `owner-alignment-record.md` O4; ADR-0006 | 2026-08-16 |
| O5 | One M0-M2 Validation Slice for one authorized Flight; two builders, 4-6 active weeks; leave 2026-08-24..09-14 excluded; end of September is a stretch target; technical completion != Committee Acceptance; Continuity Checkpoint before 2026-08-24 | `owner-alignment-record.md` O5; ADR-0007 | 2026-08-16 |
| O6 | Old SMA assets are historical candidates only; production authority + named owners decide meaning; provenance/validation/drift retained; no runtime/architecture inheritance | `owner-alignment-record.md` O6; ADR-0008 | 2026-08-16 |
| P1 | Orthogonal state dimensions, append-only revisions, Cause Verdict x Recommendation Readiness axes, Gate 0-7, human responsibility split, fail-closed policy | `wayfinder/freeze-canonical-domain-policy-contracts.md` (closed) | 2026-08-12 |
| G-Trace | Evidence Graph and execution Trace are separate linked views; Trace is not evidence | `session-records/grill-me-decision-log.md` rows "Graph and Trace", "Graph purpose" | 2026-08-12 |
| G-Mut | No state authorizes the Agent to mutate, deploy, roll back, commit, message, or publish | `session-records/grill-me-decision-log.md` row "Mutation authority" | 2026-08-12 |

## B. Observed evidence (verified by the facilitator in this job)

| ID | Observation | Evidence |
| --- | --- | --- |
| E1 | Branch `codex/kdd-data-agent-practices-research`, HEAD `28cbbda6…`, worktree heavily dirty; `docs/research/` and `.agents/skills/kdd_data_agent/` are untracked | `git status --short --branch` in this job |
| E2 | Freeze candidate SHA-256 recomputed = `40c7234f…79396` (matches handoff) | `shasum -a 256` in this job |
| E3 | Opus 5 job `671d8db1` completed 2026-08-18T02:38:55Z: `ACCEPT_WITH_CHANGES`, 3 BLOCKERs, 7 MAJORs, Phase A `PASS_WITH_GAPS` (225 tests, 19/22 mutation probes) | `m0-freeze-opus5-adversarial-review.md` (sha256 `aa0a3b5d…`), `~/.claude/jobs/671d8db1/timeline.jsonl` |
| E4 | Main session `b9d777ba` wrote a separate review to the same paths at 02:31-02:32Z (Q1-Q10, Q13 only; COI on Phase A); it was overwritten by E3 at 02:34-02:38Z. Recovered from transcript. Its surviving `m0-freeze-codex-fix-handoff.md` says "Zero blockers" | transcript `b9d777ba-….jsonl` Bash `cat >` events; recovered copy in job tmp |
| E5 | Fable adversarial job `4bda4e93` still `working` (5 lanes in flight at 02:39Z); no required outputs written | `~/.claude/jobs/4bda4e93/state.json`, dir listing |
| E6 | `.agents/skills/kdd_data_agent/.omc/state/sessions/4bda4e93-…/*.json` exist inside the package (harness state, not source) | `find` in this job |
| E7 | Ten `SEAM-M0-*` seams present and blocking; `packet_reference` strings point at superseded draft section numbers | `alignment/seams.py:105-205`; 671d8db1 MAJOR-1 |
| E8 | **Candidate bytes changed after this facilitation started.** The main orchestrator applied the eight `m0-freeze-codex-fix-handoff.md` edits plus the three 671d8db1 blocker fixes. Live digests recomputed in this job: packet `67c844d10dcafcf2637388bf579dfcd9866d7d69ea6f4d3b456fd6b6b15dfcfa`; spec `3b20c93859b78074b653d375fc29f60bd40993b3f3bcb4ee2ff481e8ba8706b8`; CE plan `52ce2763a365eb35ea884dfbc7f19b8b3d012b85edac544a21b356638126499c`; sequencing `1d42e67c0ba5e2a1799960a3e151b12fa847a419f13b7a9f63362366dc3a29ee`; eval plan `5dce10ca451f7ccbbf5d86896cefed2b8dd8a5b5cae480da4d3ea030d5603194`; planning packet `7e68ad675b988f2dfc3f53cf13e6e5e4bbd24194ce896c38df395e62b06bd37c`; owner record `c868699a60ee9a7fc72ebcc8f9345a8cb65399002812fec64d1053be2084ae6e`. All are **candidates, not frozen**. The 671d8db1 verdict binds only to the old digest `40c7234f…` and is not transferred. | `coordination-update-2026-08-17.md`; `m0-freeze-codex-disposition.md` (sha256 `5801c035…`); `shasum` in this job |
| E9 | New packet §5.1 lists arm-parity identities, "preregistered power/MDE sufficiency rule, inputs, and directional-use policy", and legal readiness-combination policy; §5.2 has 19 checks (arm parity inside check 5; check 19 = preregistered power/MDE sufficiency); §5.3 keeps two coordinated stored fields, rejects illegal pairs, no precedence, triage on `analysis_use` + `next_safe_action.kind`; §5.4 binds pre-runtime packet expiry to preregistered runtime end; header/§1 state the continuation authorization is exhausted; §13 binds the spec digest independently | packet `67c844d1…` §5, §13:327 |
| E10 | Residual wording drift after the orchestrator's edits: `spec:469` still says "only when the ExperimentReadContract permits it"; packet §11 `VAL-M0-002` still says "Runtime insufficiency" while CE plan:129 says "runtime or preregistered-power/MDE insufficiency" | `final-architecture-spec.md:469`; packet:240; CE plan:129 |
| E11 | A third, fresh Opus 5 Phase A reviewer (job `61ce23e2`, Q11/Q12) is active under `reviews/2026-08-17-phase-a-independent-verification/handoff.md`; Fable adversarial job `4bda4e93` still active. **Phase A is not cleared.** | `~/.claude/jobs/61ce23e2/state.json` (working); `~/.claude/jobs/4bda4e93/timeline.jsonl` |

## C. Reviewer findings imported (not decisions)

| ID | Finding | Source | Needs Owner? |
| --- | --- | --- | --- |
| R1 | Spent `m0-codex-continuation-20260817` authorization is presented as live at packet:7,:19 and `owner-alignment-record.md:75` | 671d8db1 BLOCKER-1 | No — **applied in candidate `67c844d1…`** (header, §1, owner record); pending fresh exact-digest review |
| R2 | `directional_only` reachable only via the runtime check; `spec:469` gates it on an undefined contract permission; a full-runtime underpowered read becomes `eligible + decision_grade` | 671d8db1 BLOCKER-2; `spec:469`; `profile:121,:386` | **Yes — Q1 → D1.** Candidate `67c844d1…` encodes it as contract field + check 19; D1 supplies the binding semantics (declared rule kinds, preregistered inputs only, no post-hoc power, MISSING-when-declared → `contract_correction`). `spec:469` clause still to be amended at writeback (E10). |
| R3 | `spec:271` requires `arm parity fields` and `legal readiness-combination policy` in `ExperimentReadContract`; packet §5.1 omits both; no M0 check consumes arm parity | 671d8db1 BLOCKER-3; `spec:271,:414` | **Yes — Q2 → D2.** Candidate `67c844d1…` matches D2 (fields in §5.1, arm parity in check 5). |
| R4 | The two readiness fields are not orthogonal: `analysis_use` fully determines `post_analysis_eligibility` | b9d777ba MAJOR-1 | Design choice. Orchestrator disposition selected **two coordinated stored fields + reject any illegal pair, no precedence** (not the derived projection) to avoid changing the propagated serialization contract. Facilitator accepts this as an engineering encoding; the architecture doc will describe eligibility as *logically* derived and the stored pair as a checked redundancy. No Owner question. |
| R5 | Materiality paragraph mixes check-outcome and materiality enums; stored `unknown` vs applied ceiling unstated | b9d777ba MAJOR-3 | No — applied in candidate (§5.2 split rules; stored `unknown`, ceiling material) |
| R6 | Pre-runtime `directional_only` packet has no expiry bound to preregistered runtime end | b9d777ba MAJOR-4 | No — applied in candidate (§5.4) |
| R7 | CE plan `VAL-M0-002` / `VAL-UNIT-001` / packet `VAL-REM-002` wording drift | b9d777ba MAJOR-2, MINOR-1/2 | No — applied in candidate; residual: packet §11 `VAL-M0-002` row lags CE plan:129 (E10) |
| R8 | `CoverageGapKind` has 9 values vs the 5-item gloss at `freeze:61`; digest-bound; neither seamed nor reported | 671d8db1 MAJOR-4; multiagent L2 | Domain-model ruling needed |
| R9 | First-screen contract dropped but still change-controlled and referenced as "accepted" in `VAL-UI-101` | 671d8db1 MAJOR-2 | No (P3-gated pointer) |
| R10 | B3 reuse inventory absent; gates `M0-F1`; whether fixtures may derive from protected assets is open | 671d8db1 MAJOR-6; `SEAM-M0-08` | **Yes** (later question) |
| R11 | 20 of 22 `VAL-*` IDs bind to no sequencing unit | 671d8db1 MAJOR-7 | No |
| R12 | Freeze binds packet digest only; M18/M19/M20 live in spec | b9d777ba MINOR-4 | No — applied in candidate (§13:327 binds spec digest independently) |


### C.2 Fable 5 Phase I findings imported (`fable5-phase1-independent-findings.md`, sealed against `40c7234f…`; job `4bda4e93`)

| ID | Finding (Phase I) | Disposition in this design |
| --- | --- | --- |
| FB-01 | §5.1 field list is a strict subset of `spec:271`; five checks have no consuming input | Design §6.2 lists every consumed field; candidate must state `spec:271` is the field authority or enumerate |
| FB-02 | Two-field readiness contract was never an Owner decision | Closed by D3 (Owner) |
| FB-03 | Spent authorization presented as live | Closed in post-edit record; design §12 |
| FB-04 | Full-runtime underpowered Flight forced to `decision_grade` | Closed by D1 (Owner) |
| FB-05 | Static scanner bypassed by attribute chains/from-imports; receipt overstates | Engineering, `fix_before_implementation`; design §4 states static assurance only |
| FB-06 | Seams cite draft sections | Engineering, Phase B binding change |
| FB-07 | `human_state` undefined | Facilitator ruling F10 (design §6.7) |
| FB-08 | No check-ID / materiality-rule registry | Facilitator ruling F11 |
| FB-09 | No closed Coverage Gap kind taxonomy | Facilitator ruling F1 |
| FB-10 | Nothing types fixture vs production packets | Facilitator ruling F12 `evidence_class`; reinforced by S2 |
| FB-11 | Open-decision register deleted | Design §16 + freeze record must carry an open-decision register with blocked-behavior mapping |
| FB-12 | Fixture derivation from old SMA open | Closed by D5 (Owner: prohibited) |
| FB-13 | Baseline rule satisfiable trivially; no precision control; too few decoys | Facilitator ruling F13 (threshold-free structure) |
| FB-14 | Phase A fixtures pooled, no per-arm data | Engineering, `M0-F5` schema requires per-arm decomposition |
| FB-15 | Recomputation independence and revalidation owner open | D4/D6 close independence; revalidation owner remains open gate (`profile` item 3) |
| FB-16 | Continuity checkpoint impossible while untracked | Hazard; Owner commit authorization required |
| FB-17 | `VAL-M0-002` freshness mapping undetermined | Facilitator ruling F14: material freshness failure → `not_permitted` |
| FB-18 | Budget cap conditional on green tests | Facilitator ruling F15: cap/expiry halt is unconditional |
| FB-19 | First-screen contract dropped | Facilitator ruling F5 |
| FB-20 | Variance-estimator validity unregistered; co-primary lacks multiplicity control | Facilitator ruling F16 |
| FB-21..25 | MISSING vs UNKNOWN; total outcome mapping; comparison rule; `decision_grade` meaning; CUPED covariate window | Facilitator rulings F17-F19 |
| FB-26..31 | Phase A test/canonicalization/toolchain minors | Engineering, Phase B |
| FB-36 | Arm parity scoping | Closed by D2 |

### C.3 Facilitator rulings (engineering / domain-model; Owner confirms at freeze)

| ID | Ruling |
| --- | --- |
| F1 | Coverage Gap `kind` is a versioned M0-contract enum (nine values frozen for M0 + `shared_source_snapshot` per D6); `freeze:61` is a concept definition; `stale_read`/`conflicting_sources` are admission-time read outcomes distinct from `evidence_state=stale` and `contradicts` |
| F3 | An insufficient (`directional_only`) packet's `expiry` ≤ preregistered sufficiency completion; reopen condition names it (already applied in candidate §5.4 for runtime) |
| F4 | Stored materiality stays `unknown`; applied ceiling material; check-outcome and materiality enums are separate rules (applied in candidate §5.2) |
| F5 | First-screen hierarchy is P3-gated; pointer to `prototype-observability-first-review-surface.md` and `spec` §14.1; `VAL-UI-101` reads "P3-accepted" |
| F7 | `VAL-*` → `M0-F*` unit binding table (design §13) |
| F8 | Freeze binds packet + spec + sequencing + eval + CE-plan digests (applied in candidate §13 for spec) |
| F10 | `human_state` = {Experiment Owner submission state, Independent DS Consultant challenge record (digest, `agree | disagree | abstain`), Committee decision `pending | pass | change | block` (real Flight only), acknowledgement + expiry}; materiality ruling actor = deterministic rule first, then preregistered rule owner; no human state changes `analysis_use` |
| F11 | Check IDs `CHK-01..CHK-19`; materiality/applicability rules live in a versioned, DS/Committee-authored registry named in the contract; `VAL-SRC-001` cites IDs |
| F12 | `evidence_class = fixture | production_authorized` on every packet and receipt, rendered on the first screen; a fixture packet can never satisfy S1 |
| F13 | Threshold-free evaluation structure: per-check planted-failure fixture + near-miss clean twin; always-ready, always-blocked, and runtime-only trivial baselines each contradicted per twin; decoys extended (pp-vs-relative swap, timezone off-by-one, exposure-trigger mismatch, stale snapshot with fresh `recorded_at`, scorecard rounding, dedup difference, arm parity, `same_pipeline` recompute) |
| F14 | Material freshness/completeness failure → `not_permitted` (never `directional_only`) |
| F15 | Cap/expiry exceeded halts unconditionally; "green command + partial packet" is the halt-state requirement, not an exemption |
| F16 | Variance-estimator validity comes from a preregistered estimator × metric-form registry reference, never the Agent; a co-primary `DecisionMetricPolicy` must state multiplicity control |
| F17 | Total mapping {check outcome × materiality} → `analysis_use` stated in design §6.4; `next_safe_action` precedence `contract_correction > evidence_collection > validity_fix > instrumentation_fix > data_quality_fix` |
| F18 | Checks 13/14 use a versioned `comparison_rule_id` owned by the `DecisionMetricPolicy`; no tolerance value invented |
| F19 | CUPED covariate window must precede exposure; metric-definition identity binds capping/winsorization/dedup rules |
| F20 | (superseded by D7 then S11) The core material-check set is the S11 fixed core plus preregistered Flight-specific applicability, sealed before the read; a core check with outcome `MISSING`/`UNKNOWN` leaves M0 capability unproven for that Flight |
| F21 | Advisory accountability: an M1 advisory always carries evidence IDs, counterevidence, an explicit falsifier, and the Independent DS Consultant's challenge record before it reaches the Committee; the advisory never renders without the official metric result beside it |
| F22 | Company-laptop boundary: the durable output is an exported packet + receipts + redaction manifest with digests; raw evidence and source snapshots stay on the laptop; deterministic replay is a laptop-side receipt; this repository holds contracts, fixtures, and exported non-sensitive packets only |
| F23 | `FlightAdvisory` is its own state dimension (`recommend_pass | recommend_change | recommend_block | insufficient_evidence`), orthogonal to Cause Verdict, Recommendation Readiness, and `analysis_use`; legal beside Cause Verdict `suspected` when the S7 floor is met; never a Recommendation item; never authorizes action |
| F24 | Orthogonal outcome-evidence streams (S7) come by preference from a preregistered, versioned stream registry with declared harm direction, declared for the Flight before the unblinding event; a stream discovered after unblinding is receipted and can carry `recommend_change`/`recommend_block` only after the S12 independent confirmation step (Owner decision S12 controls) |
| F25 | When a production source offers no immutable snapshot, Evidence Admission materializes an immutable snapshot receipt (digest, interval, scope, read set) of the primary read; the recomputation (D6) runs against that materialization; retention of the materialization follows the D8/P2 receipt |

## D. Owner decisions resolved in this facilitation

| ID | Decision | Owner answer (verbatim intent) | Source | Date |
| --- | --- | --- | --- | --- |
| D1 | **M0 readiness = validity + preregistered sufficiency, without post-hoc power computation.** `DecisionMetricPolicy` carries a preregistered `sufficiency_rule` with two allowed values now: `runtime_only` or `runtime_and_sample`. Check 2 becomes the "preregistered sufficiency" check: it compares observed runtime, and when declared, observed sample/units, against the preregistered values. Sufficiency failure with no other material blocker maps to `blocked + directional_only`. A declared `runtime_and_sample` rule whose sample fields are `MISSING` is contract-incomplete: `blocked + not_permitted` with `next_safe_action.kind = contract_correction`. `spec:469` is amended from "only when the ExperimentReadContract permits it" to "according to the sufficiency rule declared in the ExperimentReadContract". Resolves 671d8db1 BLOCKER-2 option (a) in a narrower form. **Reconciliation with candidate `67c844d1…`:** the candidate encodes sufficiency as a §5.1 field ("preregistered power/MDE sufficiency rule, inputs, and directional-use policy") and a separate check 19 while keeping check 2 for runtime. Facilitator accepts the 19-check shape as the engineering encoding; D1 fixes its meaning: (i) the rule is declared and preregistered in the `DecisionMetricPolicy` with allowed kinds `runtime_only | runtime_and_sample`; (ii) check 19 compares observed sample/units against preregistered inputs and never computes post-hoc/achieved power; (iii) under `runtime_only`, check 19 is `NOT_APPLICABLE` by that versioned rule; (iv) under `runtime_and_sample` with `MISSING` inputs the outcome is `MISSING`, contract-incomplete, `blocked + not_permitted`, `contract_correction`; (v) `spec:469` amended at writeback. | Owner selected "Include preregistered sufficiency (Recommended)" over "validity only / runtime sole trigger" and over "mandatory preregistered MDE/power". | Fable 5 facilitation Q1, AskUserQuestion | 2026-08-18 |
| D2 | **Arm parity is an M0 setup-integrity check.** Check 5 becomes "treatment/control assignment, exposure, and arm parity integrity" (per-arm index generation, serving alias, ACL snapshot, effective pipeline). `ExperimentReadContract` gains an arm-parity field group and the `legal readiness-combination policy` reference that `spec:271` already requires. Missing arm-parity fields yield check outcome `MISSING`, which under the existing rule (unknown/unclassified defaults material) gives `blocked + not_permitted` with `next_safe_action.kind = evidence_collection`. A versioned applicability rule in the `DecisionMetricPolicy` may declare the check `NOT_APPLICABLE` (for example a single-index, non-search Flight). Divergent arms are a material FAIL. Consequence for P2: per-arm index generation and ACL snapshot must be named as production evidence sources or a real Flight's M0 stays at `evidence_collection`. Resolves 671d8db1 BLOCKER-3 as "M0". | Owner selected "M0, with fields allowed to be MISSING/NA (Recommended)" over "M0 but non-blocking Coverage Gap" and over "M1". | Fable 5 facilitation Q2, AskUserQuestion | 2026-08-18 |
| D3 | **`analysis_use` is the single stored M0 readiness state; `post_analysis_eligibility` is a derived render-time projection and is never independently settable.** The three legal readings (`eligible + decision_grade`, `blocked + directional_only`, `blocked + not_permitted`) are unchanged in meaning; scenario wording may keep the pair form. Triage and any review surface key on `analysis_use` and `next_safe_action.kind`, never on eligibility alone. Packet §5.3 wording "two orthogonal fields" is replaced by "one stored state and one derived projection". Product meaning is unchanged; this is a domain-model and packet-shape ruling. Resolves b9d777ba MAJOR-1 (option a) and MINOR-3. | Owner selected "Single stored state plus derived projection (Recommended)" over "store both + reject illegal pairs". | Fable 5 facilitation Q3, AskUserQuestion | 2026-08-18 |
| D4 | **Independent recomputation (check 14) is defined by a mandatory `independence_class` on the recomputation receipt: `independent_source | independent_transform | same_pipeline`.** M0 requires at least `independent_transform` (same basis tables permitted, but separately versioned transform/join logic that does not reuse the primary pipeline code). `same_pipeline` yields check 14 = `UNKNOWN`, which under the existing materiality default blocks the packet with `next_safe_action.kind = evidence_collection`. Which class a real Flight can achieve is named by the P2 source inventory; the architecture does not invent it. Any adopted old-SMA basis-table recomputation must declare its class and carry O6 provenance. Resolves `profile:446-455` item 2. | Owner selected "Three-class enumeration, minimum independent_transform (Recommended)" over "minimum independent_source" and over "leave to P2/implementation". | Fable 5 facilitation Q4, AskUserQuestion | 2026-08-18 |
| D5 | **M0 fixtures must not derive from protected old-SMA assets.** No M0 fixture, planted truth, decoy, or manifest may be derived from `.agents/skills/sma/references/metric_registry`, `schema_catalog`, business-table routing, or old fixture facts; all M0 fixture content is synthesized from scratch. The B3 reuse inventory therefore records those assets as `read-only reference` (human reading only) for the M0 slice; `clean-room reimplement` and `direct reuse pending review` are not selected for any asset in M0. This does not change O6 for later production contracts: a validated, provenance-bearing transfer of a domain fact under P2 remains possible, but never through fixtures. Resolves `SEAM-M0-08` open Owner decision and the Owner half of 671d8db1 MAJOR-6. Facilitator recommendation (allow with provenance, P4 excluded) was **not** adopted. | Owner selected "Prohibit all derivation" over the recommended "allow with mandatory provenance, excluding P4" and over "allow without additional marking". | Fable 5 facilitation Q5, AskUserQuestion | 2026-08-18 |
| D6 | **Check-14 binding intent (refines D4).** The reported primary read and the recomputation bind to the *same immutable authoritative source snapshot*, interval, scope, and source receipt; metric meaning comes from the current production metric-definition registry/catalog and named authority, never old SMA by default; the recomputation transform is Data-Agent-owned, deterministic, independently versioned from the reporting/scorecard transform, and emits its own input manifest, transform digest, output digest, and receipt. Sharing the source snapshot is an explicit M0 boundary: check 14 can challenge transform/parameter/definition application but cannot detect corruption already in the snapshot; that limit stays visible as a typed Coverage Gap (`shared_source_snapshot`), never hidden as independence. A second independently lineaged source is optional later behind P2. No numeric tolerance is invented: the comparison rule is versioned and owned by the `DecisionMetricPolicy` (`comparison_rule_id`). Grants no P2, implementation, tolerance value, or pass verdict. | Owner selected the recommended option in a parallel thread; relayed by cross-thread handoff `kdd-m0-owner-check14-decision-20260817` (created 2026-08-17T21:18-07:00). Consistent with D4 (`independent_transform` minimum). | Cross-thread Owner handoff | 2026-08-17 |
| S1 | **Production-backed M0 Definition of Done.** M0 MVP is not complete on fixtures alone. It must run one real, authorized production Flight end to end on the company laptop and produce a reviewer-auditable `FlightReadinessPacket`. Preregistered core material checks must execute against real evidence. Unresolved items stay typed Coverage Gaps. A correctly blocked packet may prove M0 product capability; only a packet satisfying all applicable material checks may be `decision_grade` or proceed to M1. | Steelman handoff decision 1 | Owner steelman handoff `kdd-m0-steelman-owner-alignment-20260818` (relayed; confirmed by Owner chat instruction 2026-08-18) | 2026-08-18 |
| S2 | **Environment boundary.** The first real Flight can be validated only on the company laptop. Local fixtures, contracts, adapters, and deterministic replay are pre-production evidence and may not be called production validation. Table names, event schemas, threshold values, catalog identities, owners, ACLs, retention remain `PRODUCTION_BINDING_REQUIRED` until that environment is available. | Steelman handoff decision 2 | same | 2026-08-18 |
| S3 | **Decision-metric shape.** Main decision metric = online behavioral `Query Success` = union of `TraditionalResultSuccess` (result click whose click-to-next-click duration reaches a fixed threshold) and `AIAnswerSuccess` (AI-answer link click or sufficient AI-answer dwell). Thresholds are fixed within a Flight and identical across arms for the corresponding policy. Production values and remaining timer bindings deferred to the laptop. | Steelman handoff decision 3 | same | 2026-08-18 |
| S4 | **M0/M1 semantic boundary.** M0 validates the registered union formula, component instrumentation, common grain/population/window, overlap handling, production definition, source reads, reproducibility. M1 explains component movement, substitution, mix shift, user-value semantics. Component-direction conflict is not by itself an M0 validity failure. | Steelman handoff decision 4 | same | 2026-08-18 |
| S5 | **No component guardrails yet.** The union is the sole decision metric; components are diagnostic; no component-level guardrail or threshold is preregistered; the Committee makes contextual trade-off rulings. The Agent must not invent a hidden guardrail after observing results. | Steelman handoff decision 5 | same | 2026-08-18 |
| S6 | **Explicit non-binding advisory.** When components conflict, the Agent issues an explicit advisory `recommend_pass | recommend_change | recommend_block | insufficient_evidence`. It may issue `recommend_change` or `recommend_block` even when official Query Success improves and M0 confirms the calculation is valid. Official metric result and advisory stay separate; the Committee retains final authority. | Steelman handoff decision 6 | same | 2026-08-18 |
| S7 | **Evidence floor for challenging an improving official metric.** Component divergence alone triggers mandatory M1 investigation but cannot justify `recommend_change`/`recommend_block`. At least one valid, scope-matched outcome-evidence stream not mechanically derived from the same union metric is required (reformulation, abandonment, session/task outcome, downstream user action, reviewed query/result evidence, human usefulness judgment, direct user feedback). Missing evidence produces `insufficient_evidence`, never inferred absence of harm. | Steelman handoff decision 7 | same | 2026-08-18 |
| S8 | **M1 does not wait for M2.** Valid orthogonal behavioral evidence is sufficient for M1 to issue the non-binding advisory. M2 query/result examples may strengthen or falsify it but are not mandatory before M1 publishes it. | Steelman handoff decision 8 | same | 2026-08-18 |
| S9 | **M0 blocking constrains M1 claims; it does not forbid M1 investigation.** After the M0 packet is sealed the case may enter M1 even when M0 is blocked. Publication authority is evaluated per M1 claim against its evidence dependencies. `not_permitted` (invalid) supports validity/instrumentation/data-quality/remediation analysis but no treatment-causal or production-change claim that depends on the invalid comparison; `directional_only` supports explicitly directional component and orthogonal-outcome analysis; a local Coverage Gap blocks only dependent claims. M0 blockers stay visible and cannot be waived by entering M1. (Steelman handoff decision 9.) | Owner steelman handoff, updated 2026-08-18 00:30-07:00 | 2026-08-18 |
| S10 | **An invalid Flight produces a scoped, non-binding block advisory.** When M0 is `not_permitted` because the experiment is invalid, M1 emits `recommend_block` with `target = use_of_this_flight_as_decision_evidence`, `basis = invalid_experiment`, scope bound to the exact Flight generation. It advises the Committee not to rely on that Flight for the launch decision; it never blocks launch, authorizes rollback, or requests mutation. This validity-based advisory needs no orthogonal user-outcome stream; it must carry validity remediation or rerun as next safe action; Committee authority is final. (Steelman handoff decision 10.) | same | 2026-08-18 |
| S11 | **M0 material checks = fixed core + preregistered Flight-specific applicability.** Fixed core for every Flight: authorization and identity; assignment, exposure, arm parity; metric formula, components, symmetric threshold policy; population, grain, window; source authority, freshness, coverage; independent recomputation and registered statistical method; preregistered runtime/sample sufficiency. Flight-specific checks and `NOT_APPLICABLE` decisions are selected before outcome reads, bound to version and digest, justified by a versioned applicability rule; unknown applicability defaults material; checks cannot be removed after outcomes are observed (a change needs a new generation). (Steelman handoff decision 11.) **Subsumes D7**: the D7 floor is contained in this fixed core; where S11 is broader, S11 controls. | same | 2026-08-18 |
| S12 | **Post-unblinding evidence cannot alone carry `recommend_change`/`recommend_block`.** An independently lineaged outcome stream discovered after unblinding may trigger mandatory urgent investigation, but needs at least one independent confirmation (blind/preregistered reproduction; reproduction through an independently versioned data/transform path; or a named independent DS/Committee reviewer challenge confirmation with citations) before it can carry change/block. Until then the advisory is `insufficient_evidence` with next safe action `urgent_investigation`; the discovered signal, selection timing, tested-analysis inventory, counterevidence, and falsifier are preserved. (Codex continuation S9.) **Refines F24**: preregistration is the preferred path; post-unblinding streams are admissible after confirmation, not never. | `steelman-owner-alignment-codex-continuation.md` S9, Owner-confirmed 2026-08-18 | 2026-08-18 |
| S13 | **`M0 capability demonstrated` is separate from per-Flight decision-grade status.** No single `M0 complete` boolean. Capability is reached by one real authorized Flight whose fixed core checks executed, whose blocking/eligibility result was independently adjudicated as correct, and whose reviewer-auditable packet exists; a correctly blocked Flight can satisfy it. That Flight stays unavailable as Committee decision evidence; M1 claims depending on failed/missing M0 evidence stay capped; the packet records Coverage Gap `positive_production_path_unverified`; fixtures must cover the positive `decision_grade` path without being called production validation. The review surface shows program capability status and per-Flight `analysis_use` as two distinct states and never equates capability with gate passage, launch approval, production authorization, or Committee Acceptance. (Codex continuation S10.) **Refines D7/S1**: adds independent adjudication as a condition. | continuation S10, Owner-confirmed 2026-08-18 | 2026-08-18 |
| S14 | **Candidate diffs pass a separate evidence- and change-type-driven `candidate_diff_eligibility` gate.** An advisory never implies a diff. Minimum gate: exact deployed artifact identity and SHA; file/symbol attribution (line when reliable); runtime and scope reachability; supported causal mechanism; material-alternative and counterevidence challenge; independent code-domain review; LOW or MEDIUM action risk (HIGH or large blast radius cannot pass). M2 corroboration is mandatory when the change affects relevance, ranking, AI-answer behavior, presentation, or another user-visible search semantic; a versioned applicability rule may mark it not applicable for deterministic instrumentation, assignment, configuration, flag, ACL, or pipeline-wiring corrections whose evidence does not depend on query-level user-value interpretation. Diffs stay `not_applied`, generated outside a source worktree, human-only, capability-isolated, unavailable to automation. (Codex continuation S11.) | continuation S11, Owner-confirmed 2026-08-18 | 2026-08-18 |
| S-close | **Owner closed the steelman discussion** and authorized this sequence for Codex: reconcile the architecture, independent exact-byte review, complete and independently verify the M0 prototype, package research/design artifacts, verify the user-owned GitHub repository, then commit and push. This is not design approval, freeze, or implementation authority for this Fable job. | `steelman-owner-alignment-final.md` `OWNER_CONFIRMED_COMPLETE`; `fable5-budget-closeout-handoff.md` | 2026-08-18 |
| D7 | **S1 core material-check set = a preregistered subset with a fixed floor.** Floor: Flight/contract identity (CHK-01); decision-metric registration and definition version including the S3 union formula and component instrumentation (CHK-03); assignment/exposure integrity (CHK-05 core part); SRM (CHK-06); numerator/denominator/grain/unit (CHK-08); authoritative source identity (CHK-12); independent recomputation (CHK-14, D4/D6); preregistered sufficiency (CHK-19, D1); authorization/isolation (CHK-16). Arm parity (CHK-05 parity part) and CUPED (CHK-11) enter the core only when their production source is declared available before the read. The core set is preregistered in the production start receipt, versioned, and immutable after the read; non-core checks still execute or record typed Coverage Gaps. Any core check with outcome `MISSING`/`UNKNOWN` leaves M0 capability **unproven** for that Flight even when the packet is a correct `not_permitted`. Resolves the "core-check discretion" attack (F20 floor filled). | Owner selected "Preregistered subset with a fixed floor (Recommended)" over "all always-material checks are core" and over "core set decided on the laptop by P2 + DS Consultant". | Fable 5 facilitation Q6, AskUserQuestion | 2026-08-18 |
| D8 | **Minimum authorization for the first real Flight (S1) is a laptop-scoped M0 authorization receipt, not full P2 closure.** The receipt names: the Flight; the sources read and their existing owners (no new source opened); credential = the Owner's own existing read-only entitlement plus a write-denial self-attestation (no new service credential); data never leaves the company laptop; the only exports are the packet, receipts, and redaction manifest with digests; retention is laptop-local with a stated period; halt owner = Owner; security/privacy owner is informed (not a signatory). Every packet produced under it carries `authorization_scope = laptop_owner_entitlement` and can never be cited as P2 closure or system-level production authorization. Full P2 remains the gate for V2 (normalized production path) and for any second source, service credential, or export beyond the manifest. If company policy requires formal approval for any automated read, this receipt is void and the "full P2" option applies. | Owner selected "Laptop-scoped authorization receipt (Recommended)" over "P2 must fully close" and over "laptop receipt with security/privacy signature". | Fable 5 facilitation Q7, AskUserQuestion | 2026-08-18 |


### Numbering note

Two source numberings exist for the steelman decisions: the handoff file numbers 1-11 (ledger S1-S11) and the Codex continuation file numbers S9-S11 for three *different* decisions (ledger S12-S14). `steelman-owner-alignment-final.md` uses the continuation numbering. This ledger's IDs are the canonical cross-reference.

### D-notes: supersession and conflicts surfaced (facilitator, not Owner)

- **S1/S2 vs earlier "fixture-backed local M0 MVP is the first deliverable".** The latest Owner record outranks: the fixture-backed M0 (`M0-F1`-`M0-F5`) is pre-production evidence; M0 MVP *completion* requires one real authorized Flight on the company laptop. Fixture readiness, production-backed M0 capability, Flight eligibility, M1 advisory readiness, and Committee acceptance are five distinct states.
- **D3 vs post-edit packet `67c844d1…` §5.3.** The post-edit packet (edited 2026-08-17 19:44 PDT by the main orchestrator applying `m0-freeze-codex-fix-handoff.md`) encodes "two coordinated stored fields; illegal pair rejected; neither field has precedence". Owner D3 (answered later in this facilitation) selects "single stored `analysis_use`, eligibility derived at render". D3 is the later durable Owner record. Consequence: candidate v4 §5.3 must be revised before any freeze; the three legal readings are unchanged. Surfaced to the Owner in the summary; not silently resolved.
- **D1 vs post-edit packet check 19 "preregistered power/MDE sufficiency".** Compatible only if (a) `runtime_only` is a legal declared `sufficiency_rule`, and (b) the check compares preregistered inputs (runtime; when declared, sample/units) against observed values and never computes post-hoc power. Naming ("power/MDE") is acceptable; semantics must follow D1. Numbering (check 2 extended vs check 19) is editorial; this design uses "check 19 = preregistered sufficiency" to match the candidate and keeps check 2 as runtime observation input.
- **D2 vs post-edit packet check 5.** Consistent (arm parity integrated into check 5). `MISSING`/`NOT_APPLICABLE` handling per D2 must be stated in the candidate.
- **D4/D6 vs post-edit packet check 14.** Candidate still says "independently versioned recomputation path" without an independence class or shared-snapshot Coverage Gap; must be added before freeze.

## E. Open gates carried forward

P2 (production evidence authority), P3 (live review surface), P4 (evaluation gold/calibration), Committee Acceptance, new Owner start receipt for `M0-F1`–`M0-F5`, `profile:446-455` items 1-3 and 5-9, unapplied adjudications B3/M18/M19/M20.
