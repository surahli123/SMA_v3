# Fable 5 Phase I Independent Adversarial Findings — M0 Freeze Candidate and Phase A

| Field | Value |
| --- | --- |
| Reviewer | Claude Code Fable 5 (lead adversarial reviewer), model `claude-fable-5`, effort `high` |
| Handoff | `docs/research/kdd-data-agent-workshop/reviews/2026-08-17-fable5-m0-adversarial-review/handoff.md` |
| Phase | **Phase I — blind adversarial review.** Written and sealed before reading `m0-freeze-opus5-adversarial-review.md`, `m0-freeze-opus5-review-status.json`, `m0-freeze-codex-fix-handoff.md`, or `m0-multiagent-review-consolidated.md`. |
| Review window | 2026-08-17 19:27 to 20:10 America/Los_Angeles |
| Repository | `/Users/surahli/Documents/projects/SMA_v2`, branch `codex/kdd-data-agent-practices-research`, HEAD `28cbbda6e4d4d7f08134952d38433e52d3ee8768` (verified at start and at end; `git status --short` fingerprint unchanged) |
| Review object (packet) | `docs/research/kdd-data-agent-workshop/reviews/2026-08-16-opus5-m0-alignment/m0-m2-build-alignment-packet.md` at **sha256 `40c7234f3c0d85f18ebfba656e7aec2ab5ab71b86451a077ecad340eac779396`**, 29519 bytes (verified 19:27, matches handoff) |
| Phase A package | `.agents/skills/kdd_data_agent/` aggregate **sha256 `2f1001b93b19b2318c4c6419205ed2f7778ac23c02533e78a0c0899f15bf7d1e`** over 42 source/test/fixture/doc files (method reproduced, see §2) — unchanged from start to end |
| Authority | Review finding, not product authority. This document freezes nothing, authorizes nothing, and starts no M0-F1 to M0-F5 work. |

---

## 0. Read this first: the review object changed during the review

At **19:44:57 to 19:45:21 America/Los_Angeles** (after every lane had read its inputs, and while this review was still running) the following controlling files were modified on disk by another actor (a Codex fix task; `m0-freeze-codex-fix-handoff.md` appeared at 19:33:27 and was not read):

| File | Old digest (as reviewed) | New digest / size | mtime |
| --- | --- | --- | --- |
| `m0-m2-build-alignment-packet.md` | `40c7234f…` (29519 B) | `67c844d10dcafcf2637388bf579dfcd9866d7d69ea6f4d3b456fd6b6b15dfcfa` (31232 B) | 19:44:57 |
| `owner-alignment-record.md` | not recorded before change (read at 19:27) | `c868699a60ee9a7fc72ebcc8f9345a8cb65399002812fec64d1053be2084ae6e` (6305 B) | 19:44:57 |
| `eval-acceptance-plan.md` | not recorded | changed | 19:44:57 |
| `implementation-sequencing.md` | not recorded | changed | 19:44:57 |
| `final-architecture-spec.md` | not recorded | changed | 19:45:21 |
| `docs/plans/2026-08-12-001-feat-kdd-data-agent-greenfield-plan.md` | not recorded | changed | 19:45:21 |
| `planning-decision-packet.md` | not recorded | changed | 19:43:46 |

Unchanged: `enterprise-experiment-post-analysis-profile.md`, `opus5-review.md`, `freeze-canonical-domain-policy-contracts.md`, all Phase A files and fixtures.

Handling, per the handoff rule ("if the packet changes during review, stop with BLOCKED and identify the old and new digests; do not silently review mixed bytes"):

- **Every finding below is anchored to the OLD bytes** (`40c7234f…`) and to the pre-19:44:57 text of the other controlling documents. Line numbers are old-bytes line numbers.
- Nobody read the new packet bytes as a review object. Lane 2 ran a quarantined heading/term-count probe on `67c844d1…` (no verdict); it is not used here.
- Three of my own post-19:44:57 reads (`implementation-sequencing.md:11-19` authority order, `final-architecture-spec.md:859-862`, CE plan `:361`) hit new bytes. They were used only to confirm claims that Lane 2 had already made from pre-change reads, and they are marked `[post-change read]` where cited.
- The final verdict for this task is therefore **BLOCKED** (packet changed during review). Findings must be re-verified against `67c844d1…` before any disposition is acted on. Which findings are structurally likely to persist is stated in the final report, not here.

---

## 1. Method and roster

**Method.** Fable 5 personally read the handoff, Owner record, packet, planning decision packet, freeze contract, historical `opus5-review.md` (all 828 lines), continuation and pre-alignment handoffs/receipts, `TOOLCHAIN_RECEIPT.md`, `ENGINEERING_DECISIONS.md`, `README.md`, every Phase A runtime module (19), the scanner and capability tests, the fixtures, and the M0 sections of the profile, sequencing, evaluation plan and CE plan. Fable personally reran the suite (three working directories), reproduced the deterministic replay across four hash seeds, reproduced the package aggregate digest, probed the static scanner, and re-verified every BLOCKER-level anchor cited by a lane. Lanes returned conclusions and anchors only; no hidden reasoning transcripts were requested.

**Roster (5 subagents; cap respected; no workflow lanes; no nested delegation).**

| Lane | Model | Scope | Status |
| --- | --- | --- | --- |
| lane1-mechanical | Sonnet 5 (`Explore`) | VAL-ID registry, vocabulary, links, English-only, stale phrases, package inventory | Delivered (`tmp/lane-1/report.md`) |
| lane2-contract | Opus 5 (`architect`, read-only) | Owner-contract and architecture consistency; prior-finding drift; two-implementer test; freeze mechanics | Delivered (`tmp/lane-2/report.md`) |
| lane3-phasea | Opus 5 (`architect`, read-only) | Phase A code, mutation and scanner-escape testing on an isolated copy | **Did not deliver a report** (idle without output, then "session limit" failure). Its scratch scripts (`mutate.py`, `mutate2.py`, `escape.py`, `exec_proof.py`, `plant_clean.py`, `t4_digest.py`, `t6_fixture.py`) were inspected and **re-run by Fable** on the isolated copy; results are cited below as Fable-executed evidence. |
| lane4-stats-eval | Opus 5 (`architect`, read-only) | Experiment validity, statistics, fixtures, evaluation design, O6 deference | Delivered after two nudges (`tmp/lane-4/report.md`); Fable had already produced an independent substitute review of the same scope, which agrees on every major point. |
| lane5-workflow-transfer | Opus 5 (`architect`, read-only) | Enterprise workflow, production authority/ACL, continuity, KDD/DeepSeek transfer, toolchain | **Did not deliver** (idle without output, connection loss, then "session limit" failure). Fable performed this scope personally; depth is stated per finding. |

**Disclosures.**

1. *Write path.* The background-session guard rejects `Write`/`Edit` into the shared checkout and demands `EnterWorktree`. A worktree would (a) modify Git state, which the handoff forbids, and (b) not contain the untracked review directory. The three authorized artifacts were therefore authored under the job scratch directory and copied into `reviews/2026-08-17-fable5-m0-adversarial-review/` with `cp`. Only those three files were written into the repository. This mirrors the disclosed 2026-08-16 Opus session workaround.
2. *Hook side-effect inside the package.* When Bash `cd`-ed into `.agents/skills/kdd_data_agent/`, an OMC hook created `.omc/state/sessions/<session-id>/{pre-tool-advisory-throttle.json,last-tool-error-state.json}` inside the package (my session at 19:29:22-24; a lane's session at 19:53:18). No product, test, fixture, or doc byte changed (aggregate over the 42 package files stayed `2f1001b9…` throughout). I removed those hook files and the empty `.omc/` tree, one atomic command each, and re-verified the aggregate afterward. This is disclosed as an unintended write-then-restore inside a forbidden path.
3. *Independence leaks (small, disclosed).* Lane 1's `grep` for "reuse inventory" matched one line of `m0-freeze-opus5-review-status.json` (`"B3": "open - required reuse inventory not written (MAJOR-6)"`) and quoted it; that line reached me before sealing. Lane 2 reports ~10 grep-hit lines from `m0-freeze-codex-fix-handoff.md` reached it (topics: `analysis_use`/`post_analysis_eligibility` triage). All B3, readiness-mapping and materiality findings below were formed from `opus5-review.md`, the profile, the packet and the spec independently and before those hits; contamination is possible only in the sense that they are adjacent topics.
4. *Lane 4 and Lane 3 both went idle without writing a report at least once* (known OMC agent behavior); Lane 3 and 5 later failed on session limit. Nothing was invented for the missing reports.

---

## 2. Verification evidence (Fable-executed)

| Check | Command / method | Result |
| --- | --- | --- |
| Branch / HEAD | `git branch --show-current && git rev-parse HEAD` | `codex/kdd-data-agent-practices-research`, `28cbbda6e4d4d7f08134952d38433e52d3ee8768` (start and end) |
| Dirty tree | `git status --short` md5 fingerprint | `44957089267fc4deca87602f7daf3055` at start and end; 7 pre-existing worktrees, 0 stashes; nothing added, committed, stashed or checked out |
| Packet digest | `shasum -a 256 …/m0-m2-build-alignment-packet.md` at 19:27 | `40c7234f…` (matches handoff); at 19:45 `67c844d1…` (changed, see §0) |
| Package aggregate | sha256 over lines `"<sha256>  <repo-relative path>\n"` for the sorted 42 files excluding `__pycache__`/`.pytest_cache` | `2f1001b9…` — reproduces the continuation receipt claim exactly. **The receipt does not state this method**; four other plausible methods give different values (`5541bfe1…`, `4f8c6d01…`, `7c26cf56…`, `9c016a88…`). |
| Suite, repo root | `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider .agents/skills/kdd_data_agent/tests` | `225 passed in 0.24s` |
| Suite, package root | same, cwd = package | `225 passed in 0.22s` |
| Suite, external cwd | cwd = job scratch, `PYTHONPATH=…/.agents/skills` | `225 passed in 0.24s` |
| File set before/after suite | `find … -type f | sort` diff | unchanged |
| Deterministic replay | own probe `replay_probe.py`, `PYTHONHASHSEED` = 0, 1, 12345, 987654321, four clean processes | run `sha256:7837c3e6819052870299c7e0f82362680834562fb6d2e8c0f69310721c044375`, log `sha256:7221d8a458fb810839706a95e59d4004baa3d4048e4ac068a114bca8e8579ce2`, build receipt `sha256:63ba40745a6caca6f24bc7d20b29e38a18cc385c1976173c90ead7a1a6be95b1`, 47274 bytes — identical in all four processes and equal to the receipt |
| Interpreter | `python3 --version`, `pytest --version` | Python 3.14.4, pytest 9.0.2 (the only tested runtime) |
| Toolchain receipt citations | `champion-repo-reverse-audit.md:439`, `fourth-place-repo-reverse-audit.md:44,556-558,594,116,343`, `deepseek-harness-practices.md:9,99,251` | all say what `TOOLCHAIN_RECEIPT.md` says they say (5/5 spot-checks pass) |
| Isolated copy for mutation | `tmp/lane-3/kdd_data_agent`, aggregate equal to live before and after every mutation (`5541bfe1…` relative-path variant, 42 files) | live package never mutated |

Mutation results (Fable re-ran lane 3's harness on the isolated copy; each mutation reverted afterward; suite baseline 225 passed):

| Mutation | Failing tests | Reading |
| --- | --- | --- |
| a) remove `log.seal()` in runner | 10 | wired |
| b) allow append after seal | 1 | wired |
| c) drop `verify_chain` supersedes check | 1 | wired |
| d) drop `ReadResult` receipt/request digest binding | 1 | wired |
| e) drop manifest `expected_read_outcome` binding | 1 | wired |
| f) drop body-under-unauthorized guard | 2 | wired |
| g) `deep_freeze` no-op | 5 | wired |
| h) accept sets by sorting | 2 | wired |
| i) accept plain `{"__kdd__":…}` from app data | 1 | wired |
| j) `Sentinel.__bool__` returns False | 1 | wired |
| k) `stable_id` uses `id()` | 7 | wired |
| l) readiness other than ALIGNMENT_PENDING accepted | 1 | wired |
| m) `content_digest_excluding` includes excluded fields | 1 | wired |
| n) `assert_capabilities` no-op | 2 | wired |
| o) delete one planted-violation tuple | 0 (224 passed) | **not locked** — nothing asserts the planted count of 46 |
| p) `python_files` skips `adapters/` | 1 | wired |
| q) `parse_read_outcome` alias `"ok"→TRUSTED` | 1 | wired |
| r) `CoverageGap` MATERIAL without rule_source | 2 | wired |
| s1) drop `_case_path` containment (`is_relative_to`) | 0 (225 passed) | **not wired** — no symlink-escape test |
| s2) unwire `validate_case_id` | 6 | wired |
| s3) drop explicit `verify_chain()` in runner | 0 | redundant with `seal()` — acceptable |
| s4) drop duplicate-receipt-id detection in runner | 0 (225 passed) | **not wired** — receipt claims this correction has regression coverage |
| s5) drop `ReadResult` no-body guard | 1 | wired |
| s6-s8) widen body authorization / drop unauthorized-no-body / drop stale gap requirement | 2 / 2 / 2 | wired |
| s9) digest before deep_freeze | 0 | semantically equivalent — acceptable |
| s10) non-constant-time compare | 0 | not behaviorally testable — acceptable |
| s11-s17) duplicate keys, naive timestamp, loose fixture keys, readiness placeholder, plain-dict sentinel, sealed setattr, entity-kind drift | 1 / 3 / 1 / 1 / 1 / 1 / 1 | wired |
| s18-s19) scanner ignores Name loads / attribute refs | 2 / 8 | wired |
| s20) build receipt drops input digest | 1 | wired |

Static-scanner escape results (Fable-executed `scan_source` on probe snippets, then executability proofs on Python 3.14.4):

| Shape (zero findings from the scanner) | Executable proof |
| --- | --- |
| `from pathlib import os as _host` (from-import of a module through an allowed module) | `_host is sys.modules["os"]` True; `listdir('/')`, `environ.get('HOME')`, `remove`, `execv` reachable |
| `pathlib.os.remove / execv / environ / getcwd / listdir / makedirs / symlink`, `pathlib.io.FileIO(p, "w")` | `pathlib.io.FileIO(...)` **wrote 57 bytes** |
| `enum.bltns.exec("…")`, `enum.bltns.eval("…")`, `enum.bltns.__import__("…")` (`enum.bltns is builtins`) | `exec` ran; `eval('2**10')` → 1024 |
| `typing.sys.argv`, `typing.sys.path.insert`, `typing.sys.settrace` | reachable |
| `Path.walk()`, `Path.exists()`, `Path.absolute()`, `Path.samefile()` | enumeration works |
| `types.FunctionType` / `types.CodeType`, `datetime(...).astimezone()` (host-TZ dependent), `"{0.__class__}".format(...)`, f-string attribute walk, `__class__`/`__base__` singular walk, `__reduce__`, module `__getattr__`, class-body / decorator / lambda / walrus placements | reachable |
| In **test** files: aliased `path.write_text` / `read_text` / `datetime.now`, `__globals__` references | zero findings (the attribute branch is skipped for `tests/`); direct calls are caught |
| **Planted module test:** appended a scanner-clean payload (`from pathlib import os as _host`, `enum.bltns.eval`, `pathlib.io.FileIO(marker,"x")`) to `core/digest.py` **in the isolated copy** | `scan_source` → 0 findings; full suite **225 passed** including `test_runtime_package_has_no_capability_violations`; a marker file named from `HOME` length, `/` entry count and the eval result **was created**. Reverted; live package untouched. |

Digest / canonicalization probes (Fable-executed): `0.0` vs `-0.0` distinct; `10**16` int vs `1e16` float distinct; NFC vs NFD keys distinct (no collisions); floats serialize with Python `repr` (`1e+21`, `1e-07`) — not JCS/ES6 number formatting; `content_digest_excluding` equals digest of the document without the field. Fixture-adapter probes: symlink pointing outside root → blocked; in-root symlink alias → blocked by case-id/declared-id mismatch; `2026-08-16T00:00:00Z` and `…+00:00` both accepted and produce **different** digests; BOM rejected; duplicate keys rejected; `{"__kdd__":"UNKNOWN"}` in application data decodes to the sentinel (by design; encoder rejects the reverse).

Fixture arithmetic (Fable-executed): `m0-read-partial-001.json` `value` = `61252/502400` exactly while `numerator` = 61250; trusted/stale/conflicting values differ from `numerator/denominator` only at ~1e-17. All four body-bearing fixtures carry one pooled numerator/denominator with `denominator == control + treatment exposure` and no per-arm values; `assignment_unit == analysis_unit == user` with `variance_method: delta_method`.

---

## 3. Findings

Format per finding: **ID · severity · class** — claim. *Evidence.* *Affected.* *Counterargument.* *Falsifier / closure.* *Disposition · changes packet bytes.* Lane origin in brackets (`[F]` = Fable personal, `[L2]`, `[L4]`, `[L1]`, `[L3-scripts]` = lane-3 scripts re-run by Fable). Line numbers are old-bytes numbers.

### 3.1 BLOCKER

**FB-01 · BLOCKER · product defect (planning drift)** — The `ExperimentReadContract` input list in packet §5.1 (`:63-76`) is a strict subset of the spec's own entity row (`final-architecture-spec.md:271`) and of the Owner profile (`profile:84-99`); five checks have no consuming input. *Evidence.* §5.1 names no CUPED mode / covariate window / adjusted-unadjusted label (needed by check 11, `:96`), no scorecard/UI surface identity (check 13, `:98`), no independent-recomputation contract or validator identity (check 14, `:99`; `profile:97`), no numerator/denominator/grain or relative-percent-vs-percentage-point interpretation (check 8, `:93`), no attribution field (check 17, `:102`), no arm-parity fields, no monitoring/diagnostic metric labelling, and no `directional_only` permission field (`spec:469` requires one). `spec:271` lists "CUPED identity; arm parity fields; compositional-SRM plan; authoritative source, scorecard/UI reconciliation, independent recomputation, source-change revalidation … legal readiness-combination policy". `implementation-sequencing.md:14` and `CE:19` place the frozen packet at authority #2 **above** the spec, so an implementer following authority order builds the smaller contract `[post-change read confirms lane-2's pre-change reading]`. `[F][L2-04]` *Affected.* O2/O6 field binding; checks 8, 11, 13, 14, 17; `VAL-CUP-001`, `VAL-SRC-001`, `VAL-M0-001`; M0-F1/M0-F3. *Counterargument.* §5.1 says "include or explicitly mark unknown", so an implementer could mark the missing inputs `UNKNOWN`; but then checks 11/13/14 are always `UNKNOWN` → material → every Flight blocked, which is not the intended contract either. *Falsifier / closure.* §5.1 lists every field the 18 checks consume, or states that `spec:271` is the complete field authority and §5.1 is a summary. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-02 · BLOCKER · owner_decision_required** — The two-field readiness contract (`post_analysis_eligibility` × `analysis_use`, three legal pairs) and the pairing "valid pre-runtime read = `blocked + directional_only`" are not Owner decisions and the packet does not say so. *Evidence.* O1-O6 (`owner-alignment-record.md:13-52`, pre-change) contain no readiness decision. `profile:453-457` item 4 lists "whether the digest-bound alignment review accepts the proposed orthogonal mapping `post_analysis_eligibility=blocked` plus `analysis_use=directional_only` for a valid pre-runtime read" as **still to be decided**; `profile:78` calls `directional_only` "proposed". Packet §5.3 (`:109-124`) presents it as the contract and §13 (`:300`) says only that O1-O6 "are not open seams"; there is no register of decisions the Owner accepts *by freezing this packet*. `opus5-review.md:200-205` C2 asked for a third outcome state, not this shape. `[F][L2-03][L4-12]` *Affected.* §5.3, §11 (`VAL-PRE-001`, `VAL-M0-002`), M0-F1/F3, `SEAM-M0-01`. *Counterargument.* The shape is semantically sound, every downstream doc already encodes it (`seq:56`, `eval:212`, `CE:32`, `PDP:22`), and the Owner signing the packet ratifies it. *Falsifier / closure.* One Owner sentence adopting the mapping (e.g. as O7), and a "decisions resolved by accepting this packet" list in §13 covering profile items 2, 3, 4 so the Owner signs consciously. *Disposition:* `owner_decision_required` (then `fix_before_freeze` for the provenance line) · bytes **yes**.

**FB-03 · BLOCKER · production-authorization gap** — The packet binds the "current fixture-backed M0 execution" and the authorization of `M0-F1`-`M0-F5` to handoff `m0-codex-continuation-20260817`, whose one-run cap is exhausted. *Evidence.* Packet `:7`, `:19`, `:192`; `owner-alignment-record.md:75` (pre-change): "the continuation handoff permits `M0-F1`–`M0-F5` only after exact frozen packet path, revision, and SHA-256 binding". `m0-codex-continuation-receipt.md:12-14`: "This receipt exhausts the handoff's One-Run Execution Cap. This task must not resume, start M0-F1 through M0-F5, or extend scope without a new Owner authorization and handoff." `m0-codex-continuation-status.json` `next_step`: "A new Owner authorization and handoff are required for any resumed or second task." `[F][L2-01]` *Affected.* §9.1 budget rules; stop condition 6; every M0-F* start. *Counterargument.* The packet only says the handoff *permits* implementation *after* binding; the exhaustion is a task-level fact, not a packet error. But freezing bytes that read as a live authorization for spent work is exactly the stale-authority error the freeze exists to prevent, and a frozen product contract should not name a task-specific handoff at all. *Falsifier / closure.* Replace the task-specific binding with "the next implementation start receipt must bind …" and record the new authorization receipt id when the Owner issues it. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-04 · BLOCKER · product defect (statistics)** — A Flight that completed its preregistered runtime but is underpowered (traffic shortfall, low trigger rate, incomplete ramp) passes all 18 checks and is forced to `eligible + decision_grade`; the state machine forbids the correct label. *Evidence.* Packet `:105`: "Runtime insufficiency … maps specifically to `blocked + directional_only` when all other validity checks permit the read"; `:124`: `eligible + directional_only` is illegal; §5.3 `:121` admits "another frozen sufficiency rule" but none is defined anywhere; no check among 1-18 (`:86-103`) examines power, MDE, achieved exposure vs plan, or interval width; `grep -i "underpower|MDE|statistical power"` over packet, seq, CE, eval → 0 hits (Lane 2). `spec:469` (pre-change): "A valid-but-underpowered read may be labeled `directional_only` only when the ExperimentReadContract permits it" — a permission field §5.1 does not have; `spec:414` G1 lists "trigger/ramp/power". `opus5-review.md:111` M7 and `:284` C2 asked for underpowered → directional (rated BLOCKER by the original `C-causal` specialist, `opus5-review.md:552,710`). `[F][L4-01][L2-10][L2-11]` *Affected.* §5.2 check 2, §5.3, `VAL-PRE-001`, `VAL-M0-002`, M0-F3; Committee reading of "decision grade". *Counterargument.* `decision_grade` may be meant as a *validity* grade only, with power handled by the DecisionMetricPolicy downstream; but the packet never says so, and a Committee reader will hear "decision grade" as "can carry a decision". *Falsifier / closure.* Either (a) add an achieved-power / exposure-vs-plan sufficiency check and route its failure to `blocked + directional_only`, with the contract permission field, or (b) state explicitly that `decision_grade` asserts validity only, that power is out of M0 and belongs to the policy, and delete "another frozen sufficiency rule". Which one is an Owner call (profile item 1 territory). *Disposition:* `fix_before_freeze` + `owner_decision_required` · bytes **yes**.

### 3.2 MAJOR

**FB-05 · MAJOR · test weakness + receipt overstatement** — The static AST capability scanner is bypassed by attribute chains and from-imports through allowed modules; the toolchain receipt's claim that it "rejects … builtin recovery through allowed modules" is false. *Evidence.* §2 escape table (Fable-executed): `enum.bltns.exec(...)`, `pathlib.os.remove/execv/environ`, `pathlib.io.FileIO(...,"w")` (wrote 57 bytes), `from pathlib import os as _host`, `typing.sys.*`, `Path.walk` — all zero findings; a scanner-clean planted module in `core/digest.py` (isolated copy) passed the full suite (225) while reading `HOME`, listing `/`, running `eval`, and creating a file. `TOOLCHAIN_RECEIPT.md:312-320` (G-4): "The scanner now rejects the ordinary and aliased bypass corpus, including builtin recovery through allowed modules"; `m0-codex-continuation-receipt.md` §4 item 10. The receipts do also say "static source assurance … not a runtime sandbox" (`continuation-receipt` §4 end, §6). The live package contains none of these shapes (scan is clean). `[F][L3-scripts]` *Affected.* M22 / `SKEL-004` / `M0-F0` exit evidence "no … write capability is reachable"; `VAL-SEC-001` locally; stop condition 3. *Counterargument.* Every escape requires a knowing insider writing deliberately evasive code; the package has zero dependencies and no dynamic execution; the receipts label the scanner static. True — this is a defect in the *strength of the claim and the guard*, not evidence of a leak in shipped code. *Falsifier / closure.* Add a per-module attribute allowlist for stdlib module names (flag any `<allowed_module>.<attr>` outside the used set), flag `from <module> import <name>` where `<name>` is itself a module, add attribute-form `exec/eval/compile/__import__`, `remove/makedirs/symlink/link/exec*/spawn*/fork/kill/walk/listdir/scandir/getcwd/environ/getenv/putenv/FileIO/getmtime/times/settrace/argv`, apply the attribute branch to `tests/` too, and correct the receipt sentence to "rejects the named call shapes; attribute-chain and from-import smuggling are known escapes". Add a runtime import-graph assertion (`sys.modules` delta after importing the package) as a second guard. *Disposition:* `fix_before_implementation` · bytes **no**.

**FB-06 · MAJOR · planning drift (code)** — All ten `SEAM-M0-*` entries cite the *draft* packet's section numbers and describe resolved decisions as open; against `40c7234f…` 7 of 9 numeric references resolve to unrelated sections and 2 to content that no longer exists (e.g. `SEAM-M0-05` "packet §6" now points at M1 `MetricMovementPacket`; `SEAM-M0-01` still says "two outcomes (ready | blocked) or three"; `SEAM-M0-09` says Flight definition and co-primary policy "are all open"). `FrozenPacketBinding` is defined but consumed by no runtime code; the revision label `m0-alignment-v1` exists only in a test (`tests/test_alignment_seams.py:83`) and receipts. *Evidence.* `alignment/seams.py:104-206` (`packet_reference` at `:112,122,133,143,153,163,173,185,195,205`); `runner/hermetic.py:100,209` enforce these seams; Lane 1 T15, Lane 2 Q7 table. `[F][L1][L2-02][L2-21]` *Affected.* Phase B binding; `KTD16` (`CE:361` [post-change read]); §13 freeze requirement 5. *Counterargument.* Seams are meant to be replaced in Phase B in the same change that implements them; the digest binding still proves the right file. But the digest binding then proves only that the right file was opened, and an implementer resolving a seam by its stated section reaches the wrong contract. *Falsifier / closure.* Rewrite the ten `packet_reference` values against the frozen packet's headings; define the revision-label scheme in §13 and assign this candidate a label; promote the per-unit drift check (`opus5-review.md:319`; `CE:361`) into §13 as a normative freeze requirement (path + digest + revision recorded in every M0-F* exit receipt; mismatch halts the unit). *Disposition:* `fix_before_implementation` for code; `fix_before_freeze` for the §13 sentences · bytes **yes** (§13 only).

**FB-07 · MAJOR · product defect (workflow)** — "named human state" is required everywhere and defined nowhere; the O4 workflow cannot be verified against the packet. *Evidence.* Packet `:128` "human state"; `spec:274` "named human state"; `seq:59`, `eval:220`, `CE:35`. No enum for Committee decision (`pending | pass | change | block`), no field to record the Independent DS Consultant's challenge as a distinct artifact, no acknowledgement/expiry field for the M0 packet, and `VAL-CONF-001` (`:238`) "versioned ruling" and §5.2 "ruling actor" (`:105`) name no actor. The freeze contract defines `action_approval_state` and `case_state` (`freeze:81,198`) but no experiment-review state. `[F][L2-25 partial]` *Affected.* O4; §4 role table; `VAL-CONF-001`, `VAL-ROL-001`, `VAL-APR-001`, `VAL-UI-001`; M0-F4 projection. *Counterargument.* The role table (§4) plus `VAL-ROL-001` describe the workflow, and a real-Flight state can be defined at P3; but M0-F4 must render "named human state" now, so two implementers will build different things, and the DS challenge — the one artifact that makes the consultant independent — has no home. *Falsifier / closure.* Define `human_state` for the M0 packet: Experiment Owner submission state, DS challenge state (with challenge record and digest), Committee decision state (`pending | pass | change | block`, real Flight only; fixture-only overlap recorded), acknowledgement/expiry; name the materiality ruling actor (deterministic rule first; the Committee or a preregistered rule owner second); state that no human state changes the two readiness fields. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-08 · MAJOR · product defect (registries)** — Check IDs, `materiality_rule_id`, and applicability rules have no registry, authoring authority, or versioning scheme, though `M0CheckResult` requires "Check ID/version" and every `NOT_APPLICABLE`/`non_material` requires a versioned rule. *Evidence.* `spec:272` requires Check ID/version; packet `:82` "rule source", `:105` `materiality_rule_id` (its only occurrence in the corpus: `grep -rn materiality_rule_id docs .agents/skills/kdd_data_agent` → 1 hit); `grep -rn "CHK-|check_id|M0-CHK"` over the workshop corpus and CE → 0 hits; `VAL-SRC-001` (`:236`) refers to checks positionally ("check 3 or 15"). `[L2-05][L4-04][F]` *Affected.* §5.2, `VAL-SRC-001`, M0-F1/F3 packet digests, stop condition 13. *Counterargument.* An implementer can number checks in packet order and store rules as text; but two implementers then produce different packet digests, and a free-text `materiality_rule_id` makes the `non_material` gate self-certifying. *Falsifier / closure.* Assign `CHK-01..CHK-18` in the packet, name the owner and location of the materiality/applicability rule registry (engineering registry, versioned, DS/Committee-authored rules), and rewrite `VAL-SRC-001` to cite IDs. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-09 · MAJOR · product defect (taxonomy)** — No closed Coverage Gap `kind` taxonomy exists in any controlling document; the packet requires "typed Coverage Gap" and "Coverage Gap closure state" (check 18) and stop condition 1 halts on "unenumerated value". Phase A ships a 9-value `CoverageGapKind` that is a superset of the freeze contract's five (`freeze:61`) and includes `stale_read`, `conflicting_sources`, `partial_read`, `redaction_failure` (spec `:156` supports these as gaps *or blockers*); the continuation receipt never reports the disposition of its own named hypothesis "CoverageGapKind may exceed a higher-authority frozen taxonomy". *Evidence.* Packet `:82`, `:103`, `:244`, `:282`; `freeze:61`; `spec:156,258`; `core/coverage_gap.py:26-37`; `m0-codex-continuation-handoff.md` "Known hypotheses"; receipt §4/§6 silent. `[F][L2-12]` *Affected.* Check 18, `VAL-REM-001`, `SEAM-M0-03`, M0-F1. *Counterargument.* The enum is an engineering proposal at the read layer and does not decide materiality. *Falsifier / closure.* The packet (or the freeze contract via a superseding resolution) names the closed kind set for M0, or declares that the enum is engineering-owned and bound at M0-F1; the receipt records the hypothesis disposition. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-10 · MAJOR · production-authorization gap (mechanism)** — Nothing typed distinguishes a fixture packet/receipt from a production one; the O6 and P2 rules are prose only. *Evidence.* Packet §8 (`:177-181`), §11.2 last paragraph (`:276`) describe fixture authorization in prose; no required field such as `evidence_class = fixture | production_authorized` on `FlightReadinessPacket` or receipts (`spec:274`, packet `:128`); no `old_source / production_validator / validation_receipt / observed_drift` fields in §5.1 although §8 says the transferred contract records them; `grep` for `provenance_class|evidence_class|fixture_only` in spec/eval/seq → 0. Phase A's `ActorKind.FIXTURE` (`core/identity.py:44-50`) is a partial mechanism at receipt level only. `VAL-OLD-001` is gate-mapped to P2 (`:273`). `[F][L4-13]` *Affected.* O6; check 16; §11.2; `VAL-SEC-001`, `VAL-OLD-001`, `VAL-UI-001`; Committee reading of a fixture packet. *Counterargument.* Fixture packets never leave the local slice before P2. But the projection (M0-F4) is exactly what a reviewer sees, and the dangerous-green scenario is a fixture-grade or `directional_only` packet forwarded without its class label. *Falsifier / closure.* Require `evidence_class` on every packet and receipt, render it on the first screen, and add provenance/drift fields for transferred definitions to §5.1; add a synthetic `VAL-OLD-001a` (old-definition vs "production" fixture pair; production wins; drift visible) that runs locally without a production claim. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-11 · MAJOR · planning drift (freeze mechanics)** — The open-decision register was deleted rather than extended (C9 asked to *add* two decisions to it); the freeze record fields proposed by the prior review (`opus5-review.md:305-319`) — `open_decisions` with the "names the M0 behavior blocked until it resolves" rule, `party_records` "a verdict recorded against a digest other than `frozen_artifact` does not count", `drift_check` per unit — are absent from §13. *Evidence.* Packet §13 (`:298-319`); `opus5-review.md:313-319`; profile items 1-9 (`profile:453-464`) remain open. `[L2-15][F]` *Affected.* §13; freeze requirement 2 and 5. *Counterargument.* O1-O6 resolved the two decisions C9 named. But other decisions remain open (FB-02, FB-12, FB-16, FB-17) and the freeze needs a place to say which M0 behavior each blocks. *Falsifier / closure.* Add an open-decision register with blocked-behavior mapping and the two freeze-record rules. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-12 · MAJOR · owner_decision_required** — Whether M0 fixtures and the recomputation path may be derived from the protected old-SMA assets is open, and the B3 reuse inventory that was due "before `M0-F1`" does not exist while `M0-F1` is authorized. *Evidence.* `opus5-review.md:140` (B3 required action), `:341` (open decision 13); `alignment/seams.py:180-185` (`SEAM-M0-08`); `find` for `*reuse-inventory*` / `search-relevance-experiment-analysis` → nothing (Lane 1 T10); assets exist: `.agents/skills/sma/references/metric_registry/{ai_metrics,click_quality,search_success_rate}.md`, `schema_catalog/{connector_schema,search_success_rate_schema}.md`; `metric_registry`/`schema_catalog` appear in zero canonical planning documents. Packet §13 `:300` asserts O1-O6 leave no open seams; stop condition 13 fires at M0-F5. `[L2-16][L2-17][L4-13][F]` *Affected.* O6; M0-F3, M0-F5; `SEAM-M0-08`. *Counterargument.* O6 covers *production* transfer, and fixtures are synthetic; a clean-room fixture set needs no ruling. That is itself the ruling to record. *Falsifier / closure.* Owner sentence: fixtures may / may not derive from protected assets; the one-page three-column inventory. *Disposition:* `owner_decision_required` + `fix_before_implementation` · bytes **yes** (register entry).

**FB-13 · MAJOR · test weakness (evaluation design)** — The M0 baseline rule is satisfiable by one eligible and one blocked fixture; there is no false-block (precision) control; three decoy classes are too few. *Evidence.* Packet §11.1 (`:259`) "each trivial evaluator wrong on at least one case"; `eval:223` same; `eval:233` applies "materially distinguish the Agent" only to the M1 baselines; `eval:226` makes only *false readiness* a hard NO-GO, so an always-blocked stub violates zero M0 vetoes; `VAL-DECOY-001` names metric-version, CUPED-mode, source-identity only. `[F][L4-07][L4-08][L4-09]` *Affected.* §11.1, `VAL-BASE-001`, `VAL-DECOY-001`, M0-F5; handoff Q8. *Counterargument.* Numeric thresholds are P4's and must not be invented (`:255`); "threshold-free" is right for a local gate. But structure is not a threshold: per-check single-fault fixtures with paired clean twins are threshold-free and would actually discriminate. *Falsifier / closure.* Require, threshold-free, for each check a planted-failure fixture and a near-miss clean twin sharing surface features; require the always-blocked stub to be contradicted per clean twin; add a "runtime-only" trivial baseline (eligible iff runtime complete) as the M0 analogue of recent-deploy guessing; add decoys for pp-vs-relative label swap, timezone window off-by-one, exposure-trigger mismatch, stale snapshot with fresh `recorded_at`, scorecard rounding, dedup-rule difference. *Disposition:* `fix_before_freeze` (§11.1 structure) + `fix_before_implementation` (M0-F5) · bytes **yes**.

**FB-14 · MAJOR · test weakness (fixtures)** — The Phase A fixtures cannot support M0-F3/M0-F5 as they stand: all four body-bearing fixtures carry one pooled across-arm numerator/denominator/value with no per-arm data, so no treatment effect, variance, or SRM/compositional-SRM segment counts can be computed; no fixture carries CUPED, preregistered-runtime, decision-metric registration, or population fields; the trusted fixture is a pooled read declared `trusted` with `coverage_gaps: []`. `m0-read-partial-001.json` `value` equals `61252/502400` while `numerator` is 61250 (accidental arithmetic decoy). *Evidence.* §2 fixture arithmetic (Fable-executed); `evals/fixtures/m0/*.json`; receipt says fixtures are "raw source payloads, not contracts" (`m0-read-trusted-001.json` notes). `[L4-02][L4-03][L4-14][F]` *Affected.* Checks 4, 5, 6, 8, 10, 14; `VAL-M0-001`, `VAL-PRE-001`, `VAL-CUP-001`, `VAL-UNIT-001`, `VAL-SRC-001`; stop condition 5 (which the current set cannot satisfy — expected before M0-F5, but it must be said). *Counterargument.* Phase A never claimed these are the M0 acceptance fixtures; M0-F5 builds the real set. Correct — the finding is that nothing yet prevents building M0-F3 green on a substrate that has no treatment effect. *Falsifier / closure.* M0-F5 fixture schema requires per-arm decomposition; check 5 refuses a decision-metric body without it; fix or deliberately label the partial-fixture arithmetic. *Disposition:* `fix_before_implementation` · bytes **no**.

**FB-15 · MAJOR · owner_decision_required + missing evidence** — Check 14 "independently versioned recomputation path" and check 15 "source-change revalidation" depend on two decisions the profile still lists as open: how independent the recomputation path must be (item 2) and who owns semantic revalidation (item 3). *Evidence.* `profile:455-456`; packet `:99-100`; the packet never defines "independent". `[L4-11][F]` *Affected.* Checks 14, 15; M0-F3; dangerous-green scenario (recompute through the same tables/transforms/dedup → agreement proves arithmetic, not independence). *Counterargument.* A fixture slice can declare its recomputation "independent" by construction. Then say so, and name the production owner for later. *Falsifier / closure.* Owner/DS names the independence criterion (different source or transform lineage, named validator identity) and the revalidation owner; both recorded in the open-decision register. *Disposition:* `owner_decision_required` · bytes **yes**.

**FB-16 · MAJOR · production-authorization gap (continuity)** — Nothing on disk yet satisfies the Continuity Checkpoint due before 2026-08-24, and its first item cannot currently be met. *Evidence.* Packet §9.2 (`:194-207`) requires "a clean-checkout branch and immutable revision identifier after implementation authorization", one documented command, manifests, unit/scenario map, receipts, a fresh-context rehearsal and a return runbook. `git status`: the entire package and every research/plan document are **untracked** at HEAD `28cbbda`; no checkpoint artifact, runbook, or start-here index exists (Lane 1 T11: zero mentions in `README.md`/`ENGINEERING_DECISIONS.md`); a fresh engineer must read ≥14 documents (OWN, packet, PDP, FRZ, spec, seq, eval, CE, profile, README, ENGINEERING_DECISIONS, TOOLCHAIN_RECEIPT, seams.py, continuation receipt) and several superseded ones sit beside them (`handoff.md`, `m0-build-alignment-packet-draft.md`, stale seam text) with no "superseded" marker in the file itself. `[F]` *Affected.* O5; `VAL-CON-001`; `V0`. *Counterargument.* The checkpoint is a V0 deliverable that has not started; commits are gated on a separate Owner authorization. True, but the leave starts in six days and the checkpoint cannot exist without a committed revision. *Falsifier / closure.* Owner authorizes the checkpoint commit (feature branch), a start-here index names the read order and marks superseded files, and a fresh-context rehearsal receipt runs the hermetic command and names the next unit. *Disposition:* `defer_with_gate` (Owner commit authorization) + `fix_before_implementation` · bytes **no**.

**FB-17 · MAJOR · product defect (registry meaning)** — `VAL-M0-002` has two meanings under one ID and leaves a stale-but-otherwise-valid read with no determined state. *Evidence.* Packet `:232`: runtime insufficiency alone → `blocked + directional_only`; validity/source/ACL/isolation/evidence failure → `blocked + not_permitted`; "freshness" appears in the scenario column but in neither result clause. `CE:129` (pre-change read): "any material SRM, CUPED, unit, source, ACL, freshness, runtime, or comparable validity failure blocks **or** makes the Flight directional" — non-deterministic and permits `directional_only` where the packet requires `not_permitted`. `[L2-18][L4-06][F]` *Affected.* §11 registry rule "one meaning per ID"; check 9; M0-F3. *Falsifier / closure.* Align CE wording to the packet and state the freshness mapping explicitly (material freshness failure → `not_permitted`, or name it as a second sufficiency rule — which interacts with FB-04). *Disposition:* `fix_before_freeze` · bytes **yes** (packet and CE).

**FB-18 · MAJOR · product defect (budget)** — §9.1 says "An unset or exceeded cap halts that slice" (unconditional) while stop condition 6 halts only when the cap is exceeded "**without** a green hermetic command and reviewable partial packet"; green tests suspend the cap. *Evidence.* Packet `:192` vs `:288`. Inherited from C8 wording. `[L2-07]` *Affected.* §9.1, §12(6), O5 active-time envelope. *Falsifier / closure.* Make condition 6 unconditional on cap/expiry, and keep "green command + partial packet" as the *halt-state* requirement rather than an exemption. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-19 · MAJOR · planning drift** — The draft's first-screen contract and its expansion stop-gate were dropped without successors, while §13 change control and `VAL-UI-001`/`VAL-UI-101` still govern a "first-screen hierarchy". *Evidence.* Packet has no first-screen section (Lane 1 T15 heading list; `SEAM-M0-07` points at candidate §7 = M2); §13 `:319` names "first-screen hierarchy"; PDP `:23` and `spec:581-604` carry the hierarchy but the packet does not reference them; the draft §8 rule "stop after the vertical spike; obtain Owner + Opus 5 review before expanding" has no successor. `[L2-13][L2-14]` *Affected.* M0-F4, `VAL-UI-001`, §13. *Falsifier / closure.* One short section citing PDP `:23`/`spec:581-604` as the first-screen authority for `VAL-UI-001`, and a restored expansion gate. *Disposition:* `fix_before_freeze` · bytes **yes**.

**FB-20 · MAJOR · product defect (statistics)** — Check 4 / `VAL-UNIT-001` require a "named **valid**" variance estimator; naming is checkable, validity is not, and no valid estimator × metric-form registry exists (the Owner's own `search_success_rate` is a `max()` of two ratio estimators, where a plain delta method is not standard). Co-primary policy (`:42`) requires combination/conflict/stop behavior but not multiplicity control. *Evidence.* Packet `:89`, `:235`, `:42`; `.agents/skills/sma/references/metric_registry/search_success_rate.md` (Lane 4 read, head 40). `[L4-05][L4-10][F]` *Affected.* Check 4, `VAL-UNIT-001`, `VAL-MET-002`, O2. *Falsifier / closure.* "Named" + a preregistered estimator registry reference (DS/P4-owned) decides validity, never the Agent; DecisionMetricPolicy must state its multiplicity control. *Disposition:* `fix_before_freeze` (+ `owner_decision_required` for multiplicity) · bytes **yes**.

### 3.3 MINOR

**FB-21 · MINOR · planning drift** — §5.2 calls the 18 checks "the **minimum** check set" (`:84`) while `seq:58` and `SEAM-M0-02` require a *frozen* inventory; and `MISSING` vs `UNKNOWN` (`:78`) has no selection rule. `[L2-06][L2-08]` *Closure.* Replace "minimum" with "frozen"; one sentence defining `MISSING` (absent from source/contract) vs `UNKNOWN` (present but not established). `fix_before_freeze` · bytes yes.

**FB-22 · MINOR · product defect** — No total mapping from {check outcome × materiality} → readiness pair is stated in the packet (material `UNKNOWN` is covered by `spec:861` [post-change read] and `seq:58` but not by §5.3); `next_safe_action` is "exactly one kind" with no precedence rule when several apply. `[L2-09][L2-19]` *Closure.* Add the total mapping table and a precedence order (`contract_correction` > `evidence_collection` > `validity_fix` > `instrumentation_fix` > `data_quality_fix`, or as the Owner prefers). `fix_before_freeze` · bytes yes.

**FB-23 · MINOR · missing evidence (statistics)** — Checks 13/14 have no numeric comparison rule (tolerance, rounding, precision, versioned): exact equality flags correct reads at the 17th digit (see §2 fixture arithmetic), silent tolerance hides real disagreement. `[F][L4-14 adjacent]` *Closure.* Add `comparison_rule_id` to the recomputation contract, preregistered by DS/P4. `fix_before_freeze` · bytes yes.

**FB-24 · MINOR · reviewer-facing meaning** — `decision_grade` is not stated to be a validity grade only; SRM check names no test family or preregistration statement; "sealed" fixture receipt has no operational definition (digest, custody, unseal rule) and independence is optional, so the receipt is deferred evidence, not a control. `[F][L4-15][L4-16][L4-19]` *Closure.* One sentence each in §5.3, §5.2 check 6, §11.1. `fix_before_freeze` · bytes yes.

**FB-25 · MINOR · product defect (CUPED)** — Check 11 tests mode identity only; the covariate window must precede exposure (post-treatment covariate = validity failure) and `profile:93` has the field; §5.1 lacks it. Metric-definition identity should bind capping/winsorization/dedup rules (classic scorecard-vs-recompute mismatch). `[F][L4-18][L4-20]` *Closure.* Add fields to §5.1 and clauses to checks 11/12. `fix_before_freeze` · bytes yes.

**FB-26 · MINOR · test weakness (Phase A)** — Mutation survivors and scanner gaps that are not capability leaks: (a) `_case_path` containment check (`adapters/fixture.py:134`) is not wired by any test (s1: 225 passed); (b) duplicate-receipt-id detection in the runner (`runner/hermetic.py:148`) is not wired (s4: 225 passed) although the continuation receipt lists it as a corrected defect with regression coverage; (c) nothing locks the planted-violation count (o: 224 passed); (d) the scanner skips the attribute branch for `tests/`, so an aliased `write_text`/`read_text`/`datetime.now` in a test file is not caught (Fable-executed probes). `[L3-scripts][F]` *Closure.* Add a symlink-out test, a duplicate-receipt test, a `len(PLANTED_VIOLATIONS) == 46` lock, and apply the attribute branch to tests with an explicit allow for `conftest`. `fix_before_implementation` · bytes no.

**FB-27 · MINOR · code (guessing)** — `adapters/fixture.py:341-343`: when a fixture's Coverage Gap `kind` is a sentinel, the adapter substitutes `DEFAULT_GAP_KIND_BY_OUTCOME` (`blocked → missing_authority` is an inference: a read can be blocked by load or halt) with a `.get(..., UNCHECKED_PLANE)` default branch; `CoverageGap.kind` cannot itself be `UNKNOWN`. Contradicts "Preserve `UNKNOWN`; never guess" (pre-alignment handoff item 5) in spirit; also a permissive default branch (stop condition 1 class). `[F]` *Closure.* Allow `kind = UNKNOWN`, or require the fixture to state a kind. `fix_before_implementation` · bytes no.

**FB-28 · MINOR · canonicalization** — Same-instant timestamps in two forms (`…Z` vs `…+00:00`) both validate and yield different digests, contradicting "same logical value → same bytes" for time inputs; float formatting is Python `repr`, not JCS/ES6 (`1e+21`, `-0.0`), so cross-language digest agreement needs more than "swap in RFC 8785 for key ordering" (`canonical_json.py:21-24` documents only the key-order deviation; toolchain replace-trigger 2 understates it). `[L3-scripts][F]` *Closure.* Normalize timestamps to one form at validation; document float and `-0` deviations. `fix_before_implementation` · bytes no.

**FB-29 · MINOR · toolchain** — Python 3.14.4 is the only tested interpreter (`.pyc` tags `cpython-314`; `ENGINEERING_DECISIONS.md` D-2); the four replace-triggers in `TOOLCHAIN_RECEIPT.md:322-333` omit interpreter-version/host availability; corporate hosts commonly run 3.11/3.12. `[F]` *Closure.* Declare and test a supported range (e.g. 3.11-3.14) before the Continuity Checkpoint; add trigger 5 "target host lacks a supported interpreter → port". `fix_before_implementation` · bytes no. KEEP verdict on stdlib-only Python otherwise stands (see §4 Q10).

**FB-30 · MINOR · planning drift** — `spec:11-17` authority order (pre-change) omits the frozen packet, while `seq:11-19` and `CE:19` rank it #2; `VAL-REM-002` (`:240`) drops "validity" from the remediation classes O3/§5.4 include; `eval:205-232` M0 acceptance list carries no `VAL-*` IDs and digest determinism (stop condition 4) has no VAL scenario. `[F][L2-20][L2-23]` *Closure.* One line in the spec; restore "validity"; add `VAL-DET-001` and cross-reference the eval list. `fix_before_freeze` (packet/eval) · bytes yes.

**FB-31 · MINOR · receipt precision** — `__pycache__` and `.pytest_cache` exist inside the live package (created by some non-cache-disabled run), so the "zero files created" statement is a per-run claim, not a steady state; the aggregate method is unstated (see §2). `[F]` *Closure.* State the aggregate method in the receipt; add caches to a package `.gitignore` when a commit is authorized. `fix_before_implementation` · bytes no.

### 3.4 NOTE

- **FB-32 · NOTE** — Prior-finding reconciliation: C1-C9 all landed in the packet (Lane 1 T13, Lane 2 §4); M6, M22, M24, B11/C6, C4, C5, C7, C8 preserved; drift only in M7/C2 (underpowered half, FB-04) and B3 (inventory, FB-12); M1 residual in `freeze:171-174` unchanged (direction-only); `ExperimentReadContract` now in the spec entity table (`spec:271`) — the 2026-08-15 A-consistency finding is closed. `reject_finding` for anything beyond FB-04/FB-12.
- **FB-33 · NOTE** — Vocabulary hygiene is clean: no `ready | blocked` two-state text in any controlling doc; three-state materiality identical across spec/packet/seq/CE; five `NextSafeAction` kinds identical everywhere; no CJK characters; no broken relative Markdown links (Lane 1 T2-T4, T12). `FRZ` has zero occurrences of "Flight" and predates O1; not a contradiction but a superseding note is due.
- **FB-34 · NOTE** — Three-state materiality (`unknown` distinct from `material`) traces to no Owner decision; `not_applicable` is a value in four enums (M0 check, `GateReceipt.status`, Recommendation Readiness, `incident_state`) and only `unknown` is disambiguated (`profile:78`). `[L2-22][L2-24]` `defer_with_gate`.
- **FB-35 · NOTE** — `freeze:190` `Recommendation.kind` shares `validity_fix | instrumentation_fix | data_quality_fix` with `NextSafeAction.kind`; whether `InvalidExperimentRemediation` is a Recommendation item is not stated. `fix_before_implementation` wording.
- **FB-36 · NOTE** — Arm parity (M9, `spec:414`), guardrail-regression, novelty/interference/window-stability, outlier identity are in spec G1 but not in the M0 18; defensible scoping, but §5.2 should say they are M1/G1, not M0. `[L2-26][L4-17][L4-20]` `defer_with_gate`.
- **FB-37 · NOTE** — Fixture body: `variance_method: delta_method` while `assignment_unit == analysis_unit == user` and denominator = exposure; check 4's `NOT_APPLICABLE` branch is ambiguous for this shape; cross-fixture metric-version drift (`v2` vs `v3`) is not the within-fixture `VAL-SRC-001` decoy. `[L4-21][L4-22]` `fix_before_implementation`.
- **FB-38 · NOTE** — KDD/DeepSeek transfer is at the principle level (stage order in code, narrow tool surface, deterministic checks, append-only ledger, fail-closed approval, byte-identical receipts, Trace ≠ Evidence); no competition artifact, prompt card, voting or orchestration was imported; the toolchain receipt's citations check out (5/5). Most "adopted mechanisms" are ordinary software hygiene relabelled with a source — harmless, but the receipt should not present them as production evidence; the audits themselves say the evidence is competition-only. `[F]` `reject_finding` as a defect.
- **FB-39 · NOTE** — Toolchain KEEP is sound today: stdlib-only, 2,365 runtime lines, semantics in enums/frozen records/canonical JSON, replaceable ports; the receipt is not self-justifying on citations, but see FB-05 (overstated scanner claim) and FB-29 (interpreter trigger).
- **FB-40 · NOTE** — Incident: OMC hook state files were created inside the package by `cd` in Bash (mine and one lane's), then removed by me; package bytes unaffected (disclosure 2). Recommend that any future review or implementation session avoid `cd`-ing into the package or set `OMC_STATE_DIR`.
- **FB-41 · NOTE** — Continuation receipt count claims verified: 19 runtime modules, 11 test files, 225 cases, 42 non-cache files, 46 planted violations (Lane 1 T15: 2365 runtime lines / 1769 test lines match).

---

## 4. Answers to the fourteen adversarial questions (against `40c7234f…`)

1. **Owner decisions preserved without silent additions?** O1-O6 preserved (Lane 2 provenance table). One silent addition: the readiness contract and pre-runtime pairing (FB-02); three-state materiality and stop conditions 7-13 are unlabelled reviewer/orchestrator additions (FB-34).
2. **Contracts complete and mutually consistent?** No: §5.1 field set vs spec/profile (FB-01); human state undefined (FB-07); registries absent (FB-08, FB-09); freshness/underpowered mapping (FB-04, FB-17); §9.1 vs §12(6) (FB-18); otherwise readiness/materiality/`NextSafeAction`/VAL vocabulary is consistent (FB-33).
3. **Supports the real workflow?** Roles are stated correctly everywhere; the packet cannot yet *carry* the workflow because the DS challenge and Committee decision have no typed home (FB-07) and fixture packets are not typed as such (FB-10).
4. **Defers to current production schema/catalog?** In prose yes (§8, O6); by mechanism no (FB-10, FB-12, FB-15).
5. **30 + 8 findings traceable?** Yes with two drifts (FB-04 underpowered half of M7/C2; FB-12 B3 inventory) — FB-32.
6. **Phase A a real replaceable foundation?** Yes: 225 tests reproduce from three directories; deterministic replay reproduces across four hash seeds; 34 of 38 mutations are caught; digest, append-only, sealing, no-body policy, manifest binding, sentinel provenance are wired; symlink escapes blocked. Semantics-independence holds except stale seam text (FB-06), the gap-kind default (FB-27), and `AUTHORIZATION_STATES_PERMITTING_BODY` citing draft IDs (comment-only, harmless).
7. **Static checks described honestly?** Partly: "static, not a sandbox" is stated, but "rejects builtin recovery through allowed modules" is false — attribute chains, from-import smuggling, `enum.bltns.exec`, `pathlib.io.FileIO`, `Path.walk` all pass with zero findings, and a scanner-clean planted module keeps the suite green while writing a file (FB-05). No hidden clock or write exists in the shipped code.
8. **Evaluation design hard enough?** Not yet: one-ready/one-blocked satisfies the bar; no precision control; too few decoys; fixtures pooled with no per-arm data (FB-13, FB-14).
9. **KDD/DeepSeek at the right level?** Yes, principle-level; no cargo-cult artifacts (FB-38).
10. **Provisional Python sound?** KEEP, with triggers: (1) frozen packet needs a non-stdlib capability not replaceable at the adapter seam; (2) cross-language digest agreement beyond RFC 8785 key ordering (see FB-28) is required; (3) P3-accepted review surface cannot call a Python packet producer; (4) Owner/Engineering names another language; (5) **new:** target hosts lack a supported interpreter in the declared range; (6) **new:** security review requires a runtime sandbox the stdlib cannot provide (FB-05). Owners: Engineering lead for 1, 2, 5, 6; P3 owner for 3; Owner for 4.
11. **M0-F1..F5 coherent locally without pretending?** After the fix-before-freeze edits, yes; today an implementer must guess FB-01, FB-07, FB-08, FB-09, FB-21, FB-22 and cannot proceed on FB-02, FB-12, FB-15, FB-03 (authorization).
12. **Continuity for 2026-08-24 to 09-14?** Not yet possible: nothing committed, no runbook, no start-here (FB-16).
13. **Smallest evidence-backed sequence.** (0) Owner: O7 readiness adoption, fixture-derivation ruling, new start authorization; packet edits FB-01, FB-03, FB-04, FB-07..FB-11, FB-17..FB-20; rewrite seam references (FB-06). (1) M0-F1 contracts → `VAL-FLT-001`, `VAL-MET-001/002`. (2) M0-F2 reads/receipts. (3) M0-F3 decoys first: `VAL-SRC-001`, `VAL-CUP-001`, `VAL-UNIT-001`, then `VAL-PRE-001`, then `VAL-M0-002`, then `VAL-M0-001`, `VAL-CONF-001`. (4) M0-F4 seal/supersession/projection: `VAL-SUP-001`, `VAL-UI-001`, new `VAL-DET-001`; `VAL-REM-001` only. (5) M0-F5 per-arm fixtures, paired decoys, baselines: `VAL-BASE-001`, `VAL-DECOY-001`, `VAL-SEC-001`. (6) Continuity Checkpoint rehearsal `VAL-CON-001`, `VAL-APR-001`. Not in the sequence: `VAL-REM-002`, `VAL-M1/M2-*`, `VAL-ROL-001`, `VAL-OLD-001` (production), `VAL-UI-101`.
14. **Dangerous green.** (a) Full-runtime, underpowered Flight → `eligible + decision_grade` → Committee ships an unknown effect (FB-04). (b) Recomputation shares lineage with the primary read → checks 13/14 agree → false trust (FB-15). (c) Fixture-grade or `directional_only` packet rendered without an evidence-class label (FB-10). (d) A stale snapshot with a fresh `recorded_at` passes a freshness rule keyed on the receipt time (FB-13 decoy list).

---

## 5. Phase I verdict on `40c7234f…` (provisional; superseded by the BLOCKED status in the final report)

- **Freeze candidate:** not freezable as written. Four BLOCKERs, all closable by specific edits plus two Owner sentences (readiness adoption; fixture derivation) and one new start authorization. If those land, `ACCEPT_WITH_CHANGES` is the right verdict for the packet's *design*; the design is close to buildable and C1-C9 genuinely landed.
- **Phase A foundation:** `PASS_WITH_GAPS` is confirmed independently for determinism, immutability, sealing, fixture authority, read boundaries and test reachability; the capability-scanner claim must be narrowed (FB-05) and stale seam text corrected (FB-06). This is not production authorization, not Committee acceptance, and not local M0 MVP acceptance.
- **Local fixture-backed M0 MVP:** not started (M0-F1..F5 blocked at the alignment seam, and the prior authorization is exhausted).
- **Production authorization / Committee acceptance:** not granted by anything reviewed here.

Counts (this document): BLOCKER 4 · MAJOR 16 · MINOR 11 · NOTE 10 (FB-01..FB-41; FB-32/33/38/39/41 are non-defect notes).

---

## 6. Files and commands actually inspected (Fable)

Read in full: handoff; `owner-alignment-record.md` (pre-change); `m0-m2-build-alignment-packet.md` (`40c7234f…`); `planning-decision-packet.md` (pre-change); `wayfinder/freeze-canonical-domain-policy-contracts.md`; `opus5-review.md` (1-828); `m0-freeze-opus5-review-handoff.md`; `m0-codex-continuation-handoff.md`, `-receipt.md`, `-status.json`; `m0-prealignment-foundation-handoff.md`; `TOOLCHAIN_RECEIPT.md`; `ENGINEERING_DECISIONS.md`; `README.md`; all 19 runtime modules; `tests/_import_graph.py`, `tests/conftest.py`, `tests/test_capability_allowlist.py`; `manifest.json`, `m0-read-trusted-001.json`, `m0-read-blocked-001.json`; lane reports 1, 2, 4; lane-3 scripts. Read in part: profile `:20-40,80-140,440-470`; sequencing `:41-72,479-545` (pre-change) and `:11-19` [post-change]; spec `:9-20,265-276,440-460,469` (pre-change) and `:859-862` [post-change]; eval `:36-62,205-232` (pre-change); CE `:12-31,85-140` (pre-change) and `:361` [post-change]; `m0-prealignment-foundation-receipt.md:180-260`; audits at the cited lines. Commands: see §2; plus `grep`/`sed`/`ls -lT`/`shasum`/`python3` read-only probes recorded in the action ledger.

Not read (per handoff or for independence): `m0-freeze-opus5-adversarial-review.md`, `m0-freeze-opus5-review-status.json` (one line reached me via Lane 1, disclosed), `m0-freeze-codex-fix-handoff.md`, `m0-multiagent-review-consolidated.md`, the new bytes of any changed controlling document, `reviews/2026-08-17-fable5-architecture-finalization/handoff.md` (unrelated, appeared 19:36).

**Boundary statement.** No product, agent, test, fixture, controlling, or policy document was modified. No commit, branch, stash, checkout, worktree, push, PR, install, network, production, or credential access. Writes: the three authorized artifacts (via scratch + `cp`, disclosed) and the removal of hook-created `.omc` state files inside the package (disclosed, package aggregate unchanged). Mutations and planted modules were run only on the isolated copy under the job scratch directory and reverted.
